services:
  postgres:
    image: postgres:16-alpine
    container_name: alistroy-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - alistroy-net

  redis:
    image: redis:7-alpine
    container_name: alistroy-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - alistroy-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: alistroy-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      API_PORT: ${API_PORT}
      API_HOST: ${API_HOST}
      API_ENV: ${API_ENV}
      API_LOG_LEVEL: ${API_LOG_LEVEL}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_TTL_MINUTES: ${JWT_ACCESS_TTL_MINUTES}
      JWT_REFRESH_TTL_HOURS: ${JWT_REFRESH_TTL_HOURS}
      UPLOAD_DIR: ${UPLOAD_DIR}
      UPLOAD_MAX_SIZE_MB: ${UPLOAD_MAX_SIZE_MB}
      UPLOAD_PUBLIC_BASE: ${UPLOAD_PUBLIC_BASE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      RATE_LIMIT_RPS: ${RATE_LIMIT_RPS}
      RATE_LIMIT_BURST: ${RATE_LIMIT_BURST}
      MARKETPLACE_PHONE: ${MARKETPLACE_PHONE}
      MARKETPLACE_WHATSAPP: ${MARKETPLACE_WHATSAPP}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_NAME: ${ADMIN_NAME}
      PUBLIC_URL: ${PUBLIC_URL}
    volumes:
      - uploads:/app/uploads
    networks:
      - alistroy-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE}
        NUXT_PUBLIC_SITE_URL: ${NUXT_PUBLIC_SITE_URL}
        NUXT_PUBLIC_UPLOADS_BASE: ${NUXT_PUBLIC_UPLOADS_BASE}
        NUXT_PUBLIC_DEFAULT_LOCALE: ${NUXT_PUBLIC_DEFAULT_LOCALE}
    container_name: alistroy-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NUXT_PORT: ${NUXT_PORT}
      NUXT_HOST: ${NUXT_HOST}
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE}
      NUXT_PUBLIC_SITE_URL: ${NUXT_PUBLIC_SITE_URL}
      NUXT_PUBLIC_UPLOADS_BASE: ${NUXT_PUBLIC_UPLOADS_BASE}
      NUXT_PUBLIC_DEFAULT_LOCALE: ${NUXT_PUBLIC_DEFAULT_LOCALE}
    networks:
      - alistroy-net

  nginx:
    image: nginx:1.27-alpine
    container_name: alistroy-nginx
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - "${NGINX_HTTP_PORT}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - uploads:/var/www/uploads:ro
    networks:
      - alistroy-net

networks:
  alistroy-net:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  uploads:
